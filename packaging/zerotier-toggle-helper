#!/bin/bash
set -euo pipefail
iface="${1:-}"
action="${2:-}"

[[ "$iface" =~ ^zt[a-z0-9]+$ ]] || { echo "invalid interface name" >&2; exit 1; }
[[ "$action" == "up" || "$action" == "down" ]] || { echo "invalid action" >&2; exit 1; }

exec /usr/sbin/ip link set "$iface" "$action"
